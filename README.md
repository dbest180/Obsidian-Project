# Obsidian-Project
Chronicles of AZZAUM

├── .github/
│   └── workflows/
│       └── deploy.yml      # (Optional) Automated optimization/validation
├── assets/
│   ├── images/             # Lore media, background textures
│   └── styles/
│       └── main.css        # Immersive CSS layout and custom variables
├── scripts/
│   ├── modules/            # Isolated JS components (e.g., renderer, filters)
│   └── main.js             # Core application orchestrator
├── data/
│   └── lore.json           # The entire database of your universe
├── .nojekyll               # Bypasses Jekyll for instant GitHub deployments
└── index.html              # The single-page spatial canvas
